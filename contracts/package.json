{
  "name": "hashmon-contracts",
  "version": "1.0.0",
  "description": "HashMon Smart Contracts for Pharos Network",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:pharos": "hardhat run scripts/deploy.js --network pharos",
    "verify:pharos": "hardhat verify --network pharos",
    "deploy:atlantic": "hardhat run scripts/deploy.js --network pharos-atlantic"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  }
}
